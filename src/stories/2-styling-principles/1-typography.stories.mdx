import { Meta } from '@storybook/blocks';

<Meta title="2 Styling principles / 2.1 Typography" />

# 2.1 Typography

<br />
<br />

## Fonts

<br />

### Title

Montserrat => `$font-family-title`

```css
$font-family-title: 'Montserrat', sans-serif;
```

### Text

Nunito Sans => `$font-family-text`

```css
$font-family-text: 'Nunito Sans', sans-serif;
```

## Weights

```css
$font-weight-extralight: 200;
$font-weight-light: 300;
$font-weight-regular: 400;
$font-weight-semibold: 600;
$font-weight-bold: 700;
$font-weight-extra-bold: 800;
$font-weight-black: 900;
```

## Sizes

```css
$font-size-large: tools.rem(22);
$font-size-medium: tools.rem(20);
$font-size-small: tools.rem(16);
```

## Letter spacing

```css
$letter-spacing-uppercase: 0.05em;
```
