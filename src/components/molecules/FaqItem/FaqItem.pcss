.faq-item__container {
  border: none;
  padding-top: var(--space32);
  padding-left: var(--space32);
  padding-right: var(--space32);
}

.faq-item__title {
  padding-bottom: var(--space32);
  padding-right: var(--space32);
  border-bottom: solid lightgray 0.063rem;
  display: flex;
  justify-content: space-between;

  &:hover {
    cursor: pointer;
  }
}

.faq-item__title--noBorder {
  padding-bottom: var(--space32);
  padding-right: var(--space32);
  display: flex;
  justify-content: space-between;

  &:hover {
    cursor: pointer;
  }
}

.faq-item__answer {
  padding-top: var(--space32);
  padding-bottom: var(--space32);
  border-bottom: solid gray 0.063rem;
}

.faq-item__answer--with-image {
  display: grid;
  gap: var(--space-40);
  grid-template-columns: 1fr;

  @media (--mq-from-large) {
    align-items: center;
    grid-template-columns: auto 22rem;

    & .faq-item__answer__content {
      order: 0;
    }
    & .faq-item__answer__image-container {
      order: 1;
    }
  }

  @media (--mq-from-xlarge) {
    gap: var(--space-64);
    grid-template-columns: auto 28rem;
  }
}

.faq-item__answer__image-container {
  position: relative;
  height: fit-content;
}

.faq-item__answer__image {
  position: relative;
  display: block;
  overflow: hidden;
  max-width: 100%;
  height: fit-content;
  border-radius: var(--border-radius);
  filter: drop-shadow(var(--drop-shadow-lg));
}

.faq-item__answer__content {
  display: flex;
  flex-direction: column;
}

.faq-item__answer__content__buttonContainer {
  display: flex;
  justify-content: center;
}

.faq-item__answer__content__button {
  margin-top: var(--space-24);
}

.faq-item__answer--hidden {
  display: none;
}
