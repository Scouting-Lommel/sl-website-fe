.telLabel {
  display: grid;
  grid-template-rows: auto auto;
  gap: 0.2em;
}

.telInput {
  padding: 5px;
  border: 2px solid var(--color-theme-secondary);
  outline: none;
  border-radius: 5px;
  cursor: text;
}
