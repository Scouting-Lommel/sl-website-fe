.form {
  display: flex;
  flex-direction: column;
  gap: var(--space-32);
}

.form__buttons {
  display: flex;
  gap: var(--space-16);
}

.form__footnote__asterisk {
  margin-right: var(--space-4);
  color: var(--color-primary-600);
  font-size: 0.8em;
}
