export type MapEmbedProps = {
  location: string; // The address or location you want to display on the map
  width?: number; // Optional: width of the map (in pixels)
  height?: number; // Optional: height of the map (in pixels)
  title: string;
  info?: string;
};
