.article-grid {
  display: flex;
  flex-direction: column;
  margin-bottom: var(--space-80);
}

.article-grid__search {
  display: flex;
  max-width: 32rem;
  align-items: center;
  margin-bottom: var(--space-16);
  gap: var(--space-8);
}
